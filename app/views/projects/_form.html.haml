.w-full.px-4.sm:px-6.lg:px-8
  = form_with(model: project, class: "contents") do |form|
    - if project.errors.any?
      #error_explanation.bg-red-50.text-red-500.px-3.py-2.font-medium.rounded-lg.mt-3
        %h2
          = pluralize(project.errors.count, "error")
          prohibited this project from being saved:
        %ul
          - project.errors.each do |error|
            %li= error.full_message

    .my-5
      = form.label :name
      = form.text_field :name, placeholder: "Name of project or client",class: "block shadow rounded-md border border-gray-400 outline-none px-3 py-2 mt-2 w-full"

    .my-5
      = form.label :description
      = form.text_area :description, placeholder: "Description of your project",rows: 4, class: "block shadow rounded-md border border-gray-400 outline-none px-3 py-2 mt-2 w-full"

    .inline
      = form.submit class: "rounded-lg py-3 px-5 bg-blue-600 text-white inline-block font-medium cursor-pointer"
